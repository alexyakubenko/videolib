<%= simple_form_for @genre, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Genre</legend>

    <%= f.input :name %>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', genres_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
